import { Box, Flex } from "@chakra-ui/react";

export default function ReportBanner() {
    return(
        <Flex height={"400px"} style={{
            backgroundImage: "url('/img/banner_bg.png')",
            backgroundSize: "cover"
        }}>
        </Flex>
    );
}